<UserControl x:Class="MVXTester.App.Views.NodePaletteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MVXTester.App.ViewModels"
             xmlns:conv="clr-namespace:MVXTester.App.Converters">

    <UserControl.Resources>
        <conv:StringNotEmptyToVisibilityConverter x:Key="StringConv" />
    </UserControl.Resources>

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Search box -->
        <TextBox Grid.Row="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="4" Padding="4,3"
                 Background="{DynamicResource BackgroundBrush}"
                 Foreground="{DynamicResource ForegroundBrush}"
                 BorderBrush="{DynamicResource BorderBrush}">
            <TextBox.Style>
                <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                    <Style.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Tag" Value="Search nodes..." />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>

        <!-- Category tree -->
        <TreeView Grid.Row="1" ItemsSource="{Binding Categories}"
                  Background="Transparent" BorderThickness="0" Margin="0,4,0,0"
                  ItemContainerStyle="{DynamicResource ExpandableTreeViewItem}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type vm:NodeCategoryItem}"
                                          ItemsSource="{Binding Nodes}">
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <Ellipse Width="8" Height="8" Fill="{Binding CategoryColor}"
                                 VerticalAlignment="Center" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="12"
                                   Foreground="{DynamicResource ForegroundBrush}" />
                    </StackPanel>
                    <HierarchicalDataTemplate.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,1" Padding="6,3" CornerRadius="3" Cursor="Hand"
                                    Background="Transparent"
                                    MouseLeftButtonDown="NodeItem_MouseLeftButtonDown"
                                    MouseMove="NodeItem_MouseMove">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource SurfaceHoverBrush}" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="11"
                                               Foreground="{DynamicResource ForegroundBrush}" />
                                    <TextBlock Text="{Binding Description}" FontSize="9"
                                               Foreground="{DynamicResource ForegroundDimBrush}"
                                               TextWrapping="Wrap" MaxWidth="200"
                                               Visibility="{Binding Description, Converter={StaticResource StringConv}}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>
